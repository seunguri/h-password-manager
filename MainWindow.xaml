<Window x:Class="PasswordProtector.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PasswordProtector"
        mc:Ignorable="d"
        Title="계정 관리" Height="700" Width="1400" MinWidth="720"
        WindowStartupLocation="CenterScreen"
        Background="#1E1E1E"
        KeyDown="Window_KeyDown"
        PreviewKeyDown="Window_PreviewKeyDown"
        Closing="Window_Closing">
    
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <!-- 헤더 새로고침·사이드바 보내기 등 공통 테두리 버튼 -->
            <!-- 설정 패널: 체크박스와 같은 줄 높이의 보내기 행 -->
            <Style x:Key="SettingsRowExportButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Padding" Value="0"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="HorizontalAlignment" Value="Left"/>
                <Setter Property="HorizontalContentAlignment" Value="Left"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <StackPanel Orientation="Horizontal">
                                <Border x:Name="ExportGlyphBox"
                                        Width="18" Height="18"
                                        Background="#2D2D30"
                                        BorderBrush="#3F3F46"
                                        BorderThickness="1"
                                        CornerRadius="3"
                                        Margin="0,0,8,0"
                                        SnapsToDevicePixels="True">
                                    <TextBlock Text="📤"
                                               FontSize="10"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"/>
                                </Border>
                                <TextBlock Text="복호화해서 내보내기"
                                           Foreground="#DCDCDC"
                                           FontSize="12"
                                           VerticalAlignment="Center"/>
                            </StackPanel>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="ExportGlyphBox" Property="BorderBrush" Value="#007ACC"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="ToolbarOutlineButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="#2D2D30"/>
                <Setter Property="Foreground" Value="#DCDCDC"/>
                <Setter Property="Padding" Value="12,8"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="BorderBrush" Value="#3F3F46"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="4" Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="#3F3F46"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <!-- 카드 내 복사 가능한 필드(아이디·비밀번호) 공통 스타일 -->
            <Style x:Key="CopyFieldBorderStyle" TargetType="Border">
                <Setter Property="Background" Value="#2D2D30"/>
                <Setter Property="BorderBrush" Value="#3F3F46"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="CornerRadius" Value="6"/>
                <Setter Property="Padding" Value="10,8"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="#333336"/>
                        <Setter Property="BorderBrush" Value="#1C97EA"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <!-- 카드 필드 좌측 캡션(아이디/비밀번호/메모 라벨) -->
            <Style x:Key="CardFieldCaptionStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="10"/>
                <Setter Property="Foreground" Value="#6D6D70"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="Width" Value="44"/>
            </Style>

            <!-- 카드 내 필드 구분선 -->
            <Style x:Key="CardDividerStyle" TargetType="Border">
                <Setter Property="Height" Value="1"/>
                <Setter Property="Background" Value="#33373C"/>
                <Setter Property="Margin" Value="0,12,0,10"/>
            </Style>

            <!-- 카드 필드 라벨(스택형) -->
            <Style x:Key="CardStackedCaptionStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="11"/>
                <Setter Property="Foreground" Value="#8A8A8E"/>
                <Setter Property="Margin" Value="0,0,0,3"/>
            </Style>

            <!-- 카드의 핵심 동작을 명확히 드러내는 SaaS형 버튼 -->
            <Style x:Key="CardPrimaryCopyButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="#2563EB"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Padding" Value="14,11"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="CopyButtonBorder" Background="{TemplateBinding Background}"
                                    CornerRadius="8" Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="CopyButtonBorder" Property="Background" Value="#3B82F6"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="CopyButtonBorder" Property="Background" Value="#1D4ED8"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- 계정 카드 템플릿 -->
            <DataTemplate x:Key="AccountCardTemplate">
                <Border BorderBrush="#3A424E" BorderThickness="1" CornerRadius="12"
                        Margin="0,0,16,16" Width="320" Background="#252A31" MinHeight="220">
                    <StackPanel Margin="18,18">
                        <!-- 서비스명 + 상태 + 메뉴 -->
                        <Grid Margin="0,0,0,16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,6,0">
                                <Border Width="30" Height="30" Background="#303742" CornerRadius="8" Margin="0,0,10,0">
                                    <TextBlock Text="◆" Foreground="{Binding StatusColor}" FontSize="11"
                                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <TextBlock Text="{Binding ServiceName}" FontSize="15" FontWeight="SemiBold" Foreground="#F8FAFC"
                                           TextTrimming="CharacterEllipsis" VerticalAlignment="Center"/>
                            </StackPanel>
                            <Button Grid.Column="1" Content="&#xE712;" FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets"
                                    Tag="{Binding}" Click="CardMenu_Click"
                                    Foreground="#A9B3C1" FontSize="16"
                                    Width="26" Height="26" Cursor="Hand" VerticalAlignment="Top"
                                    ToolTip="수정 (삭제는 수정창 하단에서)">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border x:Name="MenuBorder" Background="Transparent" CornerRadius="4">
                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter TargetName="MenuBorder" Property="Background" Value="#3A424E"/>
                                                            <Setter Property="Foreground" Value="#DCDCDC"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </Grid>

                        <!-- 비밀번호 복사가 카드의 단일·명확한 기본 행동 -->
                        <Border Background="#1D232B" BorderBrush="#3A424E" BorderThickness="1" CornerRadius="9" Padding="12,10" Margin="0,0,0,10">
                            <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="PASSWORD" FontSize="10" FontWeight="SemiBold" Foreground="#A9B3C1" Margin="0,0,0,4"/>
                                    <TextBlock Text="{Binding PasswordDisplay}" Foreground="#E5E7EB" FontSize="14"
                                               FontFamily="Consolas, Segoe UI" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                </StackPanel>
                            <Button Grid.Column="1" Content="&#xE890;" FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets"
                                    Tag="{Binding}" Click="TogglePasswordVisibility_Click"
                                    Foreground="#A9B3C1" FontSize="16" Width="30" Height="30" Padding="0" Cursor="Hand"
                                    ToolTip="비밀번호 임시 표시/숨김">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="Transparent">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                            </Grid>
                        </Border>

                        <Button Tag="{Binding}" Click="CopyPasswordButton_Click" Style="{StaticResource CardPrimaryCopyButtonStyle}"
                                ToolTip="비밀번호를 클립보드에 복사합니다. 복사 후 원하는 곳에서 Ctrl+V를 누르세요.">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <TextBlock Text="&#xE8C8;" FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets"
                                           FontSize="15" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                <TextBlock Text="비밀번호 복사" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                        <!-- <TextBlock Text="복사 후 원하는 곳에서 Ctrl + V로 붙여넣으세요" FontSize="10.5" Foreground="#94A3B8"
                                   HorizontalAlignment="Center" Margin="0,8,0,0"/> -->

                        <!-- 아이디는 메모와 동일한 읽기 전용 정보 영역 -->
                        <StackPanel Visibility="{Binding Username, Converter={StaticResource StringToVisibilityConverter}}">
                            <Border Style="{StaticResource CardDividerStyle}"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="아이디" FontSize="12" Foreground="#8A8A8E"
                                           Width="52" VerticalAlignment="Top" Margin="0,0,10,0"/>
                                <TextBlock Grid.Column="1" Text="{Binding Username}" Foreground="#B7B7BA" FontSize="12"
                                           TextWrapping="Wrap" MaxHeight="54" TextTrimming="CharacterEllipsis"/>
                            </Grid>
                        </StackPanel>

                        <!-- 메모 (내용 있을 때만) -->
                        <StackPanel>
                            <StackPanel.Style>
                                <Style TargetType="StackPanel">
                                    <Setter Property="Visibility" Value="Visible"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding HasNotesContent}" Value="False">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </StackPanel.Style>
                            <Border Style="{StaticResource CardDividerStyle}"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="메모" FontSize="12" Foreground="#8A8A8E"
                                           Width="52" VerticalAlignment="Top" Margin="0,0,10,0"/>
                                <TextBlock Grid.Column="1" Text="{Binding NotesCardBody}" Foreground="#B7B7BA" FontSize="12"
                                           TextWrapping="Wrap" MaxHeight="54" TextTrimming="CharacterEllipsis"
                                           ToolTip="{Binding Notes}"/>
                            </Grid>
                        </StackPanel>

                        <!-- 태그 (내용 있을 때만) -->
                        <StackPanel>
                            <StackPanel.Style>
                                <Style TargetType="StackPanel">
                                    <Setter Property="Visibility" Value="Visible"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding HasTags}" Value="False">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </StackPanel.Style>
                            <Border Style="{StaticResource CardDividerStyle}"/>
                            <ItemsControl ItemsSource="{Binding Tags, Converter={StaticResource TagsToListConverter}}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel Orientation="Horizontal"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="{Binding Converter={StaticResource TagColorConverter}}" CornerRadius="5" Padding="8,3" Margin="0,0,6,5">
                                            <TextBlock Text="{Binding}" Foreground="White" FontSize="11"/>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </DataTemplate>

            <!-- "새 계정 추가" 카드 템플릿 -->
            <DataTemplate x:Key="AddCardTemplate">
                <Border BorderThickness="0" CornerRadius="12" Margin="0,0,16,16"
                        Width="320" MinHeight="220" Background="#22272F"
                        Cursor="Hand" MouseLeftButtonDown="AddCard_Click"
                        ToolTip="새 계정 등록 (Ctrl+N)">
                    <Grid>
                        <Rectangle Stroke="#4A5565" StrokeThickness="1.4" StrokeDashArray="4 3"
                                   RadiusX="12" RadiusY="12"/>
                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                            <Border Width="42" Height="42" Background="#1E3A8A" CornerRadius="12" HorizontalAlignment="Center">
                                <TextBlock Text="+" FontSize="24" FontWeight="Light" Foreground="#BFDBFE"
                                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <TextBlock Text="새 계정 추가" FontSize="14" FontWeight="SemiBold" Foreground="#E2E8F0"
                                       HorizontalAlignment="Center" Margin="0,12,0,4"/>
                            <TextBlock Text="새로운 로그인 정보를 안전하게 등록하세요" FontSize="11" Foreground="#94A3B8"
                                       HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </DataTemplate>

            <local:CardTemplateSelector x:Key="CardTemplateSelector"
                                        AccountTemplate="{StaticResource AccountCardTemplate}"
                                        AddCardTemplate="{StaticResource AddCardTemplate}"/>

            <!-- 사이드바 섹션 헤더 -->
            <Style x:Key="SidebarSectionHeaderStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="Foreground" Value="#DCDCDC"/>
            </Style>

            <!-- 사이드바 단축키 배지 -->
            <Style x:Key="ShortcutBadgeStyle" TargetType="Border">
                <Setter Property="Background" Value="#33373C"/>
                <Setter Property="BorderBrush" Value="#4A4A50"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="CornerRadius" Value="4"/>
                <Setter Property="Padding" Value="6,2"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
            </Style>
        </ResourceDictionary>
    </Window.Resources>
    
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="280"/>
        </Grid.ColumnDefinitions>
        
        <!-- Main Content -->
        <Grid Grid.Column="0" Margin="0,0,20,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Header -->
            <Grid Grid.Row="0" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <Border Width="46" Height="46" CornerRadius="11" Margin="0,0,14,0">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="#2A9DF4" Offset="0"/>
                                <GradientStop Color="#0E639C" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <Viewbox Width="26" Height="26">
                            <Canvas Width="24" Height="24">
                                <Path Fill="#EAF4FF"
                                      Data="M12,12 C14.21,12 16,10.21 16,8 C16,5.79 14.21,4 12,4 C9.79,4 8,5.79 8,8 C8,10.21 9.79,12 12,12 Z M12,14 C7.58,14 4,16.69 4,20 L4,21 L20,21 L20,20 C20,16.69 16.42,14 12,14 Z"/>
                            </Canvas>
                        </Viewbox>
                    </Border>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="계정 관리" FontSize="24" FontWeight="Bold" Foreground="#DCDCDC"/>
                        <TextBlock Text="Account Manager" FontSize="11" Foreground="#808080"/>
                    </StackPanel>
                </StackPanel>
                
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="🔄 새로고침" Click="RefreshDashboard_Click"
                            Style="{StaticResource ToolbarOutlineButtonStyle}"
                            Margin="0,0,8,0"
                            ToolTip="파일에서 계정·태그 목록을 다시 불러옵니다 (F5)"/>
                    <Button Content="+ 새 계정" Click="AddButton_Click"
                            Background="#007ACC" Foreground="White"
                            Padding="15,8" FontSize="14" BorderThickness="0"
                            Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                    CornerRadius="4" Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#1C97EA"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
            </Grid>
            
            <!-- Search Box -->
            <TextBox x:Name="SearchTextBox" Grid.Row="1" 
                     TextChanged="SearchTextBox_TextChanged"
                     KeyDown="SearchTextBox_KeyDown"
                     Background="#2D2D30" Foreground="#DCDCDC" 
                     BorderBrush="#3F3F46" BorderThickness="1"
                     Padding="10,8" FontSize="14" Margin="0,0,0,15">
                <TextBox.Style>
                    <Style TargetType="TextBox">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="TextBox">
                                    <Border Background="{TemplateBinding Background}" 
                                            BorderBrush="{TemplateBinding BorderBrush}" 
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="4">
                                        <Grid>
                                            <TextBlock x:Name="PlaceholderText" 
                                                       Text="🔍 서비스명, 아이디, 메모로 검색..."
                                                       Foreground="#808080" 
                                                       Padding="{TemplateBinding Padding}"
                                                       VerticalAlignment="Center"
                                                       IsHitTestVisible="False">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Text, RelativeSource={RelativeSource TemplatedParent}}" Value="">
                                                                <Setter Property="Visibility" Value="Visible"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                            <ScrollViewer x:Name="PART_ContentHost" Padding="{TemplateBinding Padding}"/>
                                        </Grid>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </TextBox.Style>
            </TextBox>
            
            <!-- Tag Filters -->
            <Grid Grid.Row="2" Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,14,0">
                    <Border Width="15" Height="15" BorderBrush="#5A5A60" BorderThickness="1.3" CornerRadius="3" Margin="0,0,7,0"/>
                    <TextBlock Text="필터" Foreground="#B7B7BA" FontSize="13" VerticalAlignment="Center"/>
                </StackPanel>

                <ScrollViewer Grid.Column="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                    <StackPanel Orientation="Horizontal">
                        <!-- 전체(필터 해제) -->
                        <Border x:Name="AllFilterChip" CornerRadius="5" Padding="8,3" Margin="0,0,6,5"
                                Background="#007ACC" Cursor="Hand" MouseLeftButtonDown="AllFilter_MouseLeftButtonDown">
                            <TextBlock Text="전체" Foreground="White" FontSize="11" FontWeight="SemiBold"/>
                        </Border>
                        <ItemsControl x:Name="TagFilterControl">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="{Binding Converter={StaticResource TagColorConverter}}"
                                            BorderBrush="Transparent" BorderThickness="1"
                                            CornerRadius="5" Padding="8,3" Margin="0,0,6,5"
                                            Cursor="Hand" MouseLeftButtonDown="TagFilter_MouseLeftButtonDown">
                                        <TextBlock Text="{Binding}" Foreground="White" FontSize="11"/>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </ScrollViewer>
            </Grid>
            
            <!-- Account Cards -->
            <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="AccountCardsControl"
                              ItemTemplateSelector="{StaticResource CardTemplateSelector}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
            </ScrollViewer>
            
            <!-- Footer -->
            <TextBlock x:Name="AccountCountText" Grid.Row="4" Text="총 0개의 계정" 
                       Foreground="#808080" FontSize="12" Margin="0,15,0,0" HorizontalAlignment="Left"/>
        </Grid>
        
        <!-- Right Sidebar -->
        <Border Grid.Column="1" Background="#252526" CornerRadius="8" Padding="20">
            <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="0,0,4,0">
            <StackPanel>
                <!-- 사용 가이드 헤더 -->
                <TextBlock Text="💡 사용 가이드" FontSize="16" FontWeight="Bold" Foreground="#DCDCDC" Margin="0,0,0,18"/>

                <!-- 빠른 사용법 -->
                <TextBlock Text="빠른 사용법" FontSize="12" FontWeight="SemiBold" Foreground="#8A8A8E" Margin="0,0,0,14"/>

                <!-- 새 계정 추가 -->
                <Grid Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Border Grid.Column="0" Width="30" Height="30" CornerRadius="7" Background="#2D2D30" Margin="0,0,10,0" VerticalAlignment="Top">
                        <TextBlock Text="📝" FontSize="13" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel Grid.Column="1">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="새 계정 추가" FontSize="12.5" FontWeight="SemiBold" Foreground="#DCDCDC" VerticalAlignment="Center"/>
                            <Border Grid.Column="1" Style="{StaticResource ShortcutBadgeStyle}">
                                <TextBlock Text="Ctrl + Shift + P" FontSize="10.5" Foreground="#C8C8CC" FontFamily="Consolas"/>
                            </Border>
                        </Grid>
                        <TextBlock Text="새로운 계정을 등록합니다." FontSize="11" Foreground="#808080" TextWrapping="Wrap" Margin="0,3,0,0"/>
                    </StackPanel>
                </Grid>

                <!-- 계정 검색 -->
                <Grid Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Border Grid.Column="0" Width="30" Height="30" CornerRadius="7" Background="#2D2D30" Margin="0,0,10,0" VerticalAlignment="Top">
                        <TextBlock Text="🔍" FontSize="13" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel Grid.Column="1">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="계정 검색" FontSize="12.5" FontWeight="SemiBold" Foreground="#DCDCDC" VerticalAlignment="Center"/>
                            <Border Grid.Column="1" Style="{StaticResource ShortcutBadgeStyle}">
                                <TextBlock Text="Ctrl + F" FontSize="10.5" Foreground="#C8C8CC" FontFamily="Consolas"/>
                            </Border>
                        </Grid>
                        <TextBlock Text="서비스명 또는 아이디로 검색합니다." FontSize="11" Foreground="#808080" TextWrapping="Wrap" Margin="0,3,0,0"/>
                    </StackPanel>
                </Grid>

                <!-- 비밀번호 복사 -->
                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Border Grid.Column="0" Width="30" Height="30" CornerRadius="7" Background="#2D2D30" Margin="0,0,10,0" VerticalAlignment="Top">
                        <TextBlock Text="📋" FontSize="13" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel Grid.Column="1">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="비밀번호 복사" FontSize="12.5" FontWeight="SemiBold" Foreground="#DCDCDC" VerticalAlignment="Center"/>
                            <Border Grid.Column="1" Style="{StaticResource ShortcutBadgeStyle}">
                                <TextBlock Text="Ctrl + C" FontSize="10.5" Foreground="#C8C8CC" FontFamily="Consolas"/>
                            </Border>
                        </Grid>
                        <TextBlock Text="맨 앞(선택) 계정의 비밀번호를 복사합니다." FontSize="11" Foreground="#808080" TextWrapping="Wrap" Margin="0,3,0,0"/>
                    </StackPanel>
                </Grid>

                <Border Height="1" Background="#33373C" Margin="0,20,0,16"/>

                <!-- 원본 파일 위치 -->
                <TextBlock Text="📁 원본 파일 위치" Style="{StaticResource SidebarSectionHeaderStyle}" Margin="0,4,0,10"/>
                <TextBlock Text="비밀번호는 사용자의 로컬 PC에 안전하게 저장되며, 다음 경로에서 관리됩니다."
                           FontSize="11" Foreground="#808080" TextWrapping="Wrap" LineHeight="16" Margin="0,0,0,10"/>
                <Border Background="#2D2D30" CornerRadius="6" Padding="10,8"
                        Cursor="Hand" MouseLeftButtonDown="OpenFilePath_Click">
                    <TextBlock x:Name="FilePathText"
                               FontSize="11"
                               Foreground="#6FB6E8"
                               TextWrapping="Wrap"
                               ToolTip="클릭하면 파일 위치를 엽니다"/>
                </Border>
                <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                    <TextBlock Text="🔒" FontSize="11" Margin="0,0,6,0" VerticalAlignment="Top"/>
                    <TextBlock Text="비밀번호는 암호화되어 안전하게 보관됩니다."
                               FontSize="11" Foreground="#808080" TextWrapping="Wrap"/>
                </StackPanel>

                <!-- 설정 -->
                <Border Height="1" Background="#33373C" Margin="0,20,0,16"/>
                <TextBlock Text="⚙️ 설정" Style="{StaticResource SidebarSectionHeaderStyle}" Margin="0,0,0,14"/>
                
                <CheckBox x:Name="StartupCheckBox" 
                          Content="Windows 시작 시 자동 실행" 
                          Foreground="#DCDCDC" 
                          FontSize="12"
                          Margin="0,0,0,12"
                          Checked="StartupCheckBox_Checked"
                          Unchecked="StartupCheckBox_Unchecked"
                          Loaded="StartupCheckBox_Loaded">
                    <CheckBox.Style>
                        <Style TargetType="CheckBox">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="CheckBox">
                                        <StackPanel Orientation="Horizontal">
                                            <Border x:Name="CheckBoxBorder" 
                                                    Width="18" Height="18" 
                                                    Background="#2D2D30" 
                                                    BorderBrush="#3F3F46" 
                                                    BorderThickness="1" 
                                                    CornerRadius="3"
                                                    Margin="0,0,8,0">
                                                <TextBlock x:Name="CheckMark" 
                                                           Text="✓" 
                                                           Foreground="#007ACC" 
                                                           FontSize="12" 
                                                           FontWeight="Bold"
                                                           HorizontalAlignment="Center" 
                                                           VerticalAlignment="Center"
                                                           Visibility="Collapsed"/>
                                            </Border>
                                            <ContentPresenter VerticalAlignment="Center"/>
                                        </StackPanel>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsChecked" Value="True">
                                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                                                <Setter TargetName="CheckBoxBorder" Property="BorderBrush" Value="#007ACC"/>
                                            </Trigger>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="CheckBoxBorder" Property="BorderBrush" Value="#007ACC"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </CheckBox.Style>
                </CheckBox>

                <Button Click="ExportDecryptedIni_Click"
                        Style="{StaticResource SettingsRowExportButtonStyle}"
                        Margin="0,0,0,8"
                        ToolTip="비밀번호를 평문으로 풀어 INI 파일로 저장합니다."/>

                <!-- <Border Height="1" Background="#33373C" Margin="0,20,0,16"/> -->

                <!-- 상태 표시 -->
                <!-- <TextBlock Text="🕘 상태 표시" Style="{StaticResource SidebarSectionHeaderStyle}" Margin="0,4,0,14"/>

                <Grid Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Ellipse Grid.Column="0" Width="9" Height="9" Fill="#4EC9B0" Margin="0,3,10,0" VerticalAlignment="Top"/>
                    <StackPanel Grid.Column="1">
                        <TextBlock Text="정상" FontSize="12.5" FontWeight="SemiBold" Foreground="#4EC9B0"/>
                        <TextBlock Text="정상적으로 사용할 수 있는 계정입니다." FontSize="11" Foreground="#808080" TextWrapping="Wrap" Margin="0,2,0,0"/>
                    </StackPanel>
                </Grid>

                <Grid Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Ellipse Grid.Column="0" Width="9" Height="9" Fill="#E5C07B" Margin="0,3,10,0" VerticalAlignment="Top"/>
                    <StackPanel Grid.Column="1">
                        <TextBlock Text="7일 이내 만료" FontSize="12.5" FontWeight="SemiBold" Foreground="#E5C07B"/>
                        <TextBlock Text="비밀번호가 7일 이내 만료됩니다." FontSize="11" Foreground="#808080" TextWrapping="Wrap" Margin="0,2,0,0"/>
                    </StackPanel>
                </Grid>

                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Ellipse Grid.Column="0" Width="9" Height="9" Fill="#F48771" Margin="0,3,10,0" VerticalAlignment="Top"/>
                    <StackPanel Grid.Column="1">
                        <TextBlock Text="만료 / 기한 초과" FontSize="12.5" FontWeight="SemiBold" Foreground="#F48771"/>
                        <TextBlock Text="비밀번호가 만료되었거나 기한이 초과되었습니다." FontSize="11" Foreground="#808080" TextWrapping="Wrap" Margin="0,2,0,0"/>
                    </StackPanel>
                </Grid> -->

            </StackPanel>
            </ScrollViewer>
        </Border>

        <!-- 복사 성공 알림 오버레이 -->
        <Border x:Name="CopyToastBorder" Grid.ColumnSpan="2"
                Background="#007ACC" CornerRadius="6" Padding="16,10"
                HorizontalAlignment="Center" VerticalAlignment="Bottom"
                Margin="0,0,0,24" IsHitTestVisible="False"
                Visibility="Collapsed" Opacity="0">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="📋" FontSize="14" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <TextBlock x:Name="CopyToastText" Text="비밀번호가 복사되었습니다"
                           Foreground="White" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
